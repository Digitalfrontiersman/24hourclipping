# ---- Frontend environment ----
# Copy to frontend/.env and adjust if needed.

# Base URL of the backend API (no trailing slash). The app calls `${URL}/api`.
# Local dev / Docker Compose both expose the backend on host port 8001.
REACT_APP_BACKEND_URL=http://localhost:8001

# Dev server port (optional).
PORT=3000
