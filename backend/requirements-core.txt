# Minimal, PyPI-installable dependencies to run the core API
# (everything except the optional AI concierge endpoints).
# The full pinned set — including the Emergent AI stack — lives in requirements.txt.
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
motor==3.3.1
pymongo==4.6.3
pydantic==2.13.4
# PostgreSQL (async ORM + driver + migrations) — target datastore
SQLAlchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.1
python-dotenv==1.2.2
python-multipart==0.0.32
# Auth
PyJWT==2.13.0
bcrypt==4.1.3
email-validator==2.3.0
# Google OAuth verification (optional feature; imported lazily)
google-auth==2.55.2
requests==2.34.2
# Card payments (Stripe Checkout)
stripe==14.4.1
# Solana USDC payments (escrow, payouts, tips)
solana==0.36.6
solders==0.26.0
# AI concierge (OpenAI)
openai==1.99.9
# S3-compatible object storage (AWS S3 / R2 / B2 / Storj)
boto3==1.35.99
